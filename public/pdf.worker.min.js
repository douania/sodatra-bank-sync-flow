
// PDF.js Worker - Local fallback
// This is a minimal worker implementation to avoid CDN issues
self.onmessage = function(e) {
  // Simple worker that just passes messages back
  // The actual PDF.js worker will be loaded dynamically if available
  self.postMessage(e.data);
};
